package ua.hillel.java.elementary1.arrays.tasks;

/**
 * Remove duplicate.
 */
public interface RemoveDuplicates {
    /**
     * Remove duplicates from provided arrays and return new array containing only unique values.
     *
     * @param numbers the numbers
     * @return the array containing no duplicates.
     */
    int[] removeDuplicates(int[] numbers);
}
